Fixed the Zoom In and Zoom Out toolbar/menu icons being swapped: Zoom In (which increases the zoom level) showed a magnifier with a minus glyph and Zoom Out (which decreases it) showed a magnifier with a plus glyph, backwards from convention and from what the actions do; the two icons are now assigned to match their actions
