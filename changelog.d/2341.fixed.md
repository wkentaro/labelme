Fixed the outline of a Mask shape being drawn one pixel down-and-right of its filled pixels; the contour is traced on a 1-pixel-padded copy of the mask (so a region touching the border still closes), but that padding offset was never subtracted before positioning the stroke, unlike the polygon path that shares the same trick, so the outline now aligns with the fill it encloses
