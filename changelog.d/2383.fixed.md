Fixed a naturally-spaced `--labels`/`--flags` comma list such as `--labels "cat, dog, person"` registering labels with leading spaces (`" dog"`, `" person"`); the inline comma-split branch now strips whitespace around each entry and drops blank entries, matching the file-reading branch's existing convention, so a later `--validate-label exact` no longer rejects `"dog"` because the registered label was actually `" dog"`
